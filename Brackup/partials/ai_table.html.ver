<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Client IA</th>
                <th>Modelo</th>
                <th class="text-end">Ações</th>
            </tr>
        </thead>
        <tbody>
            {% for ai in ai_clients %}
            <tr style="cursor: pointer;" onclick="window.location='{% url 'ai_config:ai_edit' ai.id %}'">
                <td>{{ ai.name }}</td>
                <td>{{ ai.ai_client.api_client_class }}</td>
                <td>{{ ai.model_name|default:"Padrão" }}</td>
                <td class="text-end">
                    <div class="btn-group">
                        <button onclick="event.stopPropagation(); confirmDelete('{{ ai.id }}', '{{ ai.name }}')" class="btn btn-sm btn-outline-danger">
                            <i class="bi bi-trash"></i>
                        </button>
                    </div>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

{% if ai_clients.has_other_pages %}
<div class="card-footer">
    <nav>
        <ul class="pagination justify-content-center m-0">
            {% if ai_clients.has_previous %}
            <li class="page-item">
                <a class="page-link" href="?page={{ ai_clients.previous_page_number }}" aria-label="Anterior">
                    <span aria-hidden="true">&laquo;</span>
                </a>
            </li>
            {% endif %}

            {% for num in ai_clients.paginator.page_range %}
            <li class="page-item {% if ai_clients.number == num %}active{% endif %}">
                <a class="page-link" href="?page={{ num }}">{{ num }}</a>
            </li>
            {% endfor %}

            {% if ai_clients.has_next %}
            <li class="page-item">
                <a class="page-link" href="?page={{ ai_clients.next_page_number }}" aria-label="Próximo">
                    <span aria-hidden="true">&raquo;</span>
                </a>
            </li>
            {% endif %}
        </ul>
    </nav>
</div>
{% endif %}

<style>
.table thead th {
    border-bottom: none;
    font-weight: 500;
}

.badge {
    font-size: 0.85em;
    padding: 0.4em 0.6em;
}

.btn-group .btn {
    padding: 0.25rem 0.5rem;
}

.table td {
    vertical-align: middle;
}
</style>
